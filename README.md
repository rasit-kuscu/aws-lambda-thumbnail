# aws-lambda-thumbnail

npm install --production

aws lambda function description should be

```json
{
  "width":320,
  "api": {
    "url": "http://",
    "username": "rasit",
    "password": "123456"
    }
}
```
